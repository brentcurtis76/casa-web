SESSION: BILINGUE · D1a-2 · r1 · EXEC

You are the executor for CASA **BILINGUE**, phase **D1a-2**, attempt 1. You have no prior context;
everything you need is here or at the paths named below. This is a **durable** conversation — keep
it open until Codex passes the phase.

---

## 0. Read these first, in this order

1. `~/.claude/agent-workflow/LEAN-WORKFLOW.md` — **ACTIVE**, and it is an instruction to you, not
   background. In particular §4.2 (you are the durable executor), §5 (stop rules), §6 (the ledger
   block you must write).
2. `docs/plan/bilingue/PLAN.md` — **§ "Phase D1a-2 — Amend the method" is your contract.** Also read
   "Frozen architectural decisions" (D-A … D-O) and the Decision log's five rows dated 2026-08-12.
3. `docs/plan/bilingue/LEDGER.md` — the last two entries: D1b-1 round 3 (**FINDINGS**) and the PM
   re-plan. They are why this phase exists.
4. `docs/plan/bilingue/evidence/SURFACE-SCHEMA.md` — the artifact you are amending.
5. `CLAUDE.md` at the repo root. Its hard rules outrank everything in this prompt.

---

## 1. Where you are working — verify before you edit

```bash
cd /Users/brentcurtis/dev/casa-pilot
git rev-parse --show-toplevel                              # /Users/brentcurtis/dev/casa-pilot
git rev-parse --path-format=absolute --git-common-dir      # /Users/brentcurtis/dev/casa-web/.git
git worktree list
```

- **WORKTREE:** `/Users/brentcurtis/dev/casa-pilot`. **Not `casa-web`** — that checkout is on
  `plan/bilingue` and has no `docs/plan/bilingue/` directory at all. A previous executor lost time
  to exactly this.
- **BRANCH:** create `phase/d1a2-method` from **`phase/d1b1-output` @ `5f8de92`**.

  ```bash
  git switch -c phase/d1a2-method phase/d1b1-output
  ```

  **Not from `pilot/sop-v2`** — that branch does not carry D1b-1's ledger, census, exclusions or
  triage. This is a logged decision, not an accident.
- **Commit as you go.** Nothing merges to `pilot/sop-v2` in this phase. Do not merge, do not push to
  `main`, do not deploy, do not touch the database.

---

## 2. Why this phase exists

D1b-1 tried to produce the surface inventory and was blocked after three attempts: Codex FAIL(4) →
all four fixed → FAIL(2), both in the same categories. Overlay §5 fired. Three findings came back,
and the PM reproduced all three independently:

- **F1 — a criterion demanded a record the schema forbids.** `[D1b.5]`/`[D1b1.8]` required an
  emission record for `src/lib/whatsapp/templates.ts`. It emits nothing: no importer anywhere in
  `src` or `supabase`, and **no edge function imports from `src/` at all**. *Already fixed by the PM
  in the plan — your job is the schema-side rule, [D1a2.2].*
- **F2 — `text-origin` has no truthful value for platform-locale output.** Three records carry
  `literal in source` while their own `reason` says the text is CLDR data, not a repository string.
- **F3 — the method fixes a record's shape but never how to derive the set.** `§Record syntax`
  splits records by *"different text origins, **emissions**, sinks, or language-axis sets"* —
  `emission` is undefined and is the term carrying the entire rule. Two rounds of hand-written
  records split inconsistently in both directions because of it.

F2 and F3 live inside `SURFACE-SCHEMA.md`, which is hash-locked by `METHOD-MANIFEST.txt`. `[D1b.1]`
and **D-L** put it out of a results phase's reach — which is why this method phase exists.

**D-L, the decision this whole workstream is built on:** *method before measurement.* A counting or
classification method is committed and independently reviewed in a phase that produces **no
results**, before the phase that produces results. **You produce no records and no counts.**

---

## 3. Scope — six artifacts

| # | Path | What |
|---|---|---|
| 1 | `docs/plan/bilingue/evidence/SURFACE-SCHEMA.md` | amended in place — F1, F2, F3 |
| 2 | `docs/plan/bilingue/evidence/derive-sites.sh` | new, pure shell — derives the emission-**site** set |
| 3 | `docs/plan/bilingue/evidence/validate-records.sh` | new, pure shell — validates a records file against it |
| 4 | `docs/plan/bilingue/evidence/fixtures/schema/` | new — fixtures proving both scripts in both directions |
| 5 | `docs/plan/bilingue/evidence/SCHEMA-CHANGELOG.md` | new — what changed, why, error direction, what it does not fix |
| 6 | `docs/plan/bilingue/evidence/METHOD-MANIFEST.txt` | regenerated over the full method |

Plus your ledger entry and the review-request artifact in §7.

### Explicitly out of scope — producing any of these is a BLOCKING finding

- Any surface record. Any count, total, or measurement result.
- Any edit to `census.sh`, `CENSUS-METHOD.md` or `wordlist-passB.txt` — the census is not in
  question and D1b-1's committed census output must stay valid.
- Any edit to `D1-surfaces-output.md`, `D1-sink-triage.md`, `D1-exclusions.md`,
  `D1-census-raw.txt`, `D1-census-stderr.txt` or `fixtures/D1-census.json`.
- Running `derive-sites.sh` or `validate-records.sh` against `D1-surfaces-output.md`. That is
  D1b-1's first gate; running it here is measuring inside a method phase (D-L).
- Any source, schema or config change (**D-A**). Any recommendation about how a surface should be
  made bilingual (**D-E**) — this phase describes, it does not prescribe.
- Re-litigating what Codex explicitly preserved in D1b-1: the audit yield, the `print` finding, the
  nine blind spots, the Group 3 exclusion, and rows #30/#32. Those stand.

---

## 4. Acceptance criteria

**[D1a2.1] — [D1a2.12] in `PLAN.md` § "Phase D1a-2" are the contract. Read them there; they are not
paraphrased here.** Four notes on the ones most likely to be got wrong:

- **[D1a2.1] (F2).** Add **one** scalar for text produced by the runtime's locale data. The
  discriminating test is that its emitted vocabulary is not enumerable from this repository at all.
  Then state the contrast explicitly: `wa-reminders/index.ts:24-31` hardcodes its Spanish month
  array in source and therefore **stays** `literal in source`. Both emit Spanish month names; only
  one is a repository string. That contrast is the criterion, not decoration.
- **[D1a2.3] (F3).** Define `emission` operationally, fix the reconciliation key
  **route × sink × origin × axis**, and state the split/merge rule **once** so it is not re-decided
  per record.
- **[D1a2.6].** One valid fixture that exits 0, and **one deliberately broken fixture per rule** in
  [D1a2.5], each exiting non-zero and naming the rule it broke. A validator nobody has watched fail
  is not a validator.
- **[D1a2.8].** No artifact may contain a record count, and **no rule, threshold or fixture may be
  justified by how many records it yields.** You may read `D1-sink-triage.md`, `D1-exclusions.md`,
  the census outputs, and `D1-surfaces-output.md` **§§0, 1, 5, 6**. §§2–4 and §7 are a blocked
  draft, not a target. The reviewer has been asked to look specifically for a derivation shaped to
  reproduce them.

---

## 5. Gates — run every one, paste raw output

The full block is in `PLAN.md` § "Test plan — D1a-2". Run it verbatim. Summary:

1. census artifacts byte-identical to `dee6a1a` — `git diff --stat` prints nothing.
2. `bash -n` on both scripts; then `validate-records.sh` against **fixtures only** — valid exits 0,
   every `invalid-*` exits non-zero.
3. `METHOD-MANIFEST.txt` regenerated; `shasum -a 256 -c` → all OK.
4. the no-results grep prints `OK: no results in D1a-2`.
5. `git diff --stat phase/d1b1-output...HEAD` → only `docs/plan/bilingue/`;
   `git diff --quiet e0c9342 -- src supabase` → exit 0.

`lint`, `test`, `build`, Playwright and `tsc` are **excluded by contract**: the source diff is zero
lines (D-A), and `npx tsc --noEmit` compiles zero files — the root `tsconfig.json` is a solution
file with `"files": []`. The real check fails on the base with 1,039 errors across 122 files:
repository debt, base-red under overlay §5, not this phase's. Baseline:
`evidence/BASE-GATES-d5df247.md`. Do not add a gate the contract excluded, and do not drop one it
requires.

**`2>/dev/null` anywhere in this phase is a BLOCKING finding.** It discarded exactly the evidence a
previous phase needed. If you must show the prohibition in prose, that is fine; no *command* may use it.

**A red required gate is `BLOCKED`, never complete** (overlay §5). Report it with the real output.

---

## 6. Rules

- **D-B / D-M:** every claim carries the exact runnable command that produced it, executed, with its
  literal output pasted. Never describe a command in prose. A summary of what a command did is not
  evidence that it did it — this is the failure that produced six FAILed plan reviews.
- **D-K measurement hygiene:** absolute binary paths for **every** process in a pipeline
  (`/usr/bin/grep`, `/usr/bin/find -E`, `/usr/bin/sort`, `/usr/bin/wc`, `/usr/bin/comm`),
  `LC_ALL=en_US.UTF-8` **exported once** rather than prefixed on the first command. `grep` and
  `find` are shell functions in this environment; unqualified calls do not reproduce.
- **D-O:** no artifact asserts a safety property it has not tested. Every rule states its **error
  direction**, and where evidence is ambiguous it fails toward **inclusion, in code**. Neither script
  may claim the record set is complete — it is consistent with a **derived floor**, and the floor is
  a floor (D-J).
- **Privacy (CASA hard rule, outranks this prompt):** record paths, symbols, shapes and counts —
  **never personal-data values**. Celebrant, preacher, musician and recipient names and phone
  numbers are described by role and never quoted. Fixtures use synthetic data only.
- Surgical changes. Match the existing artifacts' idiom — read `SURFACE-SCHEMA.md` and `census.sh`
  before writing, and follow their conventions rather than importing your own.
- Do not grade your own work in prose. Show the diff and the raw output.
- **Spanish is CASA's default language** for output and commit messages, matching this workstream's
  existing commits.

### The escape hatch — this is a success, not a failure

If you conclude the contract is wrong, **stop and report `FINDINGS`** rather than working around it.
The specific case the PM expects: **[D1a2.3] may not be achievable.** If `emission` cannot be
defined precisely enough that two people split the same composite payload the same way, say so and
propose the boundary as a question for Brent — do **not** invent a rule only its author can apply.
Inventing one is what produced F3. This is D-N applied to the method (D-O).

---

## 7. When the gates are green

1. Write `docs/plan/bilingue/reviews/D1a2-r1-review-request.md` — the artifact Codex reviews
   against. Follow `reviews/D1b1-r1-review-request.md` for shape: criterion-by-criterion, each with
   the command and its literal output, plus **the weakest part of your diff and what you could not
   establish**. Name blind spots; do not let the reviewer discover them.
2. Append your round entry to `docs/plan/bilingue/LEDGER.md` using the overlay §6 block —
   `STARTED` / `ENDED` as real ISO-8601 timestamps you actually observed, `ATTEMPT: 1`,
   `RISK: HIGH`, `HANDOFFS`, `GATES`, `CODEX: pending`, `ESCAPED DEFECT: n/a`.
3. Commit, then output the executor report (`AGENT-WORKFLOW.md` §2.3 format) and stop.

The PM routes the cumulative diff `phase/d1b1-output...phase/d1a2-method` to one independent Codex
review. **Findings come back into this same conversation as one deduplicated batch** (overlay §4.2)
— do not open a new session, and do not start a remediation round before the verdict arrives.

---

## 8. Known blind spots — stated so you do not rediscover them as surprises

- **That F1–F3 are the only method defects is unestablished.** Two review rounds found three; a
  third could find more. This is the accepted risk in dispatching this phase.
- **`derive-sites.sh` inherits D-J's problem rather than solving it.** It is a regex-derived floor
  paired with the call-path audit, and that regex was wrong by **3×** once already — it missed a
  live WhatsApp reply. If you write the script as though it replaces the audit, D1b-1 will
  under-report and look rigorous doing it.
- **The PM did not enumerate every `toLocaleString`-family call site.** F2's artifact names three;
  there may be more. Write the rule, not the list.
- **`[D1a2.3]` is this phase's hardest criterion** — it is `[D1a.5]`'s difficulty one level down,
  and `[D1a.5]` was the hardest thing this workstream has done. Budget for it accordingly.
